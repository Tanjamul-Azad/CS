# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# The earlier exploratory notebook in this project had keys pasted directly
# into cells. Those keys are revoked. Do not repeat that pattern.

GROQ_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# For the D1 registry harvest (raises GitHub API rate limits)
GITHUB_TOKEN=
